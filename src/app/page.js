"use client";

import Button from "@/components/Button";
import React from "react";

const Home = () => {
  return (
    <div>
      <h1>Next JS hadir di UTY</h1>
      <Button title={"BTR"} onClick={() => {}} />
    </div>
  );
};

export default Home;
